package ActiveEdge.Parser;

public class Parser {
    public void handleInput(String input) {
        if(input.length() >= 1){
            System.out.println("Hello " + input);
        }
    }
}
