package logic;

import model.Order;

public class OrderLogic {
    public static Order createNewOrder() {
        Order newOrder = new Order();
        boolean isExit = false;
        while(!isExit) {
            //TODO: Add menu items to order
        }
        return newOrder;
    }
}
